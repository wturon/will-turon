Type: AWS::Amplify::Branch
Properties:
  AppId: !GetAtt AdminApp.AppId
  BranchName: ${opt:stage}
  Stage: !If [IsProd, PRODUCTION, DEVELOPMENT]
  EnableAutoBuild: true
  EnvironmentVariables:
    - Name: REACT_APP_ENVIRONMENT
      Value: ${opt:stage}
